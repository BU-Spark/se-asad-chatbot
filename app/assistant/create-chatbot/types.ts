export interface AssistantFormData {
  name: string;
  description: string;
  instructions: string;
}

export interface ExampleInstruction {
  title: string;
  description: string;
  instructions: string;
}
